SELECT D.name AS Department, E.name AS Employee, E.salary AS Salary FROM Employee AS E JOIN Department AS D ON E.departmentId = D.id  WHERE 3 > (SELECT COUNT(DISTINCT E2.salary) FROM Employee AS E2 WHERE E2.salary > E.salary AND E.departmentId = e2.departmentId);
