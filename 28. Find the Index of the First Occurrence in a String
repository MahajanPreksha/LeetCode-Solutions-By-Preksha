class Solution {
public:
    int strStr(string haystack, string needle) {
        if(needle.length()==0){
            return 0;
        }
        int idx = haystack.find(needle);
        if(idx==-1){
            return -1;
        }
        return idx;
    }
};
